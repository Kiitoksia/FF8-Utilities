<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FF8Utilities.MAUI.Pages.MainPage"
             x:Name="this"             
             BindingContext="{x:Reference this}"
             Title="MainPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Text="Card Tracker" Command="{Binding CardTrackerCommand}"/>
        <Button Text="Fish Fins" Grid.Column="1"/>
        <Button Text="Caraway Code" Grid.Row="1"/>
        <Button Text="Ultimecia Party" Grid.Row="1" Grid.Column="1"/>

        <ActivityIndicator IsRunning="{Binding ShowLoadingBar}" IsVisible="{Binding ShowLoadingBar}" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
    </Grid>
</ContentPage>
