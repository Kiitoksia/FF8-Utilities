<mah:MetroWindow
    x:Class="FF8Utilities.Dialogs.QuistisCardPatternWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:converters="clr-namespace:FF8Utilities.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
    xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:encs="clr-namespace:FF8Utilities.Entities.Encounters"
    xmlns:extensions="clr-namespace:FF8Utilities.Extensions"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:ff8Utilities="clr-namespace:FF8Utilities"
    xmlns:local="clr-namespace:FF8Utilities.Dialogs"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:FF8Utilities.Models"
    xmlns:quistis="clr-namespace:LateQuistisManipulation.Models;assembly=LateQuistisManipulation"
    ResizeMode="NoResize"
    Height="730"
    Topmost="True"
    WindowStartupLocation="CenterOwner"
    x:Name="Window"
    xmlns:controls1="clr-namespace:FF8Utilities.Controls"
    d:DataContext="{d:DesignInstance quistis:LateQuistisPattern}"
    Width="620"
    mc:Ignorable="d"
    Title="{Binding RNGIndex, StringFormat='Quistis Card Pattern (RNG Index: {0})'}">
    <mah:MetroWindow.Resources>
        <ff8Utilities:ByteArrayToImageConverter x:Key="ImageConverter"/>
    </mah:MetroWindow.Resources>
    <TabControl ItemsSource="{Binding Strategies}" TabStripPlacement="Left" Style="{DynamicResource MahApps.Styles.TabControl.Animated}" SelectedIndex="0">
        <TabControl.ItemContainerStyle>
            <Style TargetType="TabItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Border" BorderThickness="5" BorderBrush="Transparent" Background="Transparent">
                                <Grid>
                                    <ItemsControl ItemsSource="{Binding OpponentCards}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Image Source="{Binding ., Converter={StaticResource ImageConverter}}" Height="68"/>
                                                    <Border x:Name="ImageBorder" Background="Black">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=IsSelected}" Value="True">
                                                                        <Setter Property="Opacity" Value="0"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=IsSelected}" Value="False">
                                                                        <Setter Property="Opacity" Value="0.25"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>
                                                </Grid>
                                                
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{Binding Frame, StringFormat={}Frame {0}}"
                                           VerticalAlignment="Bottom"
                                               HorizontalAlignment="Left"
                                           Foreground="White"
                                           Background="#7F000000"
                                               Padding="2"
                                           FontSize="16"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="5"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="False">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="Transparent"/>
                                    <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </TabControl.ItemContainerStyle>
        <TabControl.ContentTemplate>
            <DataTemplate>
                <Grid Width="460">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="600"/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <ItemsControl ItemsSource="{Binding Positions}" Grid.Row="1" AlternationCount="20">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls1:QuistisCardPatternCardPosition DataContext="{Binding .}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{Binding Frame, StringFormat={}Frame {0}}" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold"/>
                    <Button Grid.Row="2" Content="SUBMIT" Style="{DynamicResource MahApps.Styles.Button.Dialogs.Accent}" Command="{Binding SubmitCommand, ElementName=Window}" CommandParameter="{Binding .}"/>
                </Grid>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</mah:MetroWindow>