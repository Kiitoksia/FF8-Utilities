<UserControl x:Class="FF8Utilities.Controls.SettingsInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FF8Utilities.Controls"
             mc:Ignorable="d" 
             x:Name="root"
             
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="4" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentPresenter Content="{Binding LabelContent, ElementName=root}" Margin="4" VerticalAlignment="Center"  HorizontalAlignment="Center"/>
        <ContentPresenter Content="{Binding InputContent, ElementName=root}" Margin="4" Grid.Column="1" VerticalAlignment="Center" />
    </Grid>
</UserControl>
