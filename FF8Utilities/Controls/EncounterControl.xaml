<UserControl
    x:Class="FF8Utilities.Controls.EncounterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:FF8Utilities.Extensions"
    xmlns:local="clr-namespace:FF8Utilities.Controls"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:FF8Utilities.Models"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance models:BaseEncounterModel}"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <GroupBox Header="{Binding Description}">
        <StackPanel>
            <ItemsControl ItemsSource="{Binding Abilities}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="models:EncounterAbilityModel">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Description}" />
                            <mah:NumericUpDown
                                Grid.Column="1"
                                Value="{Binding Count}"
                                Minimum="0" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid mah:VisibilityHelper.IsVisible="{Binding FanfareVisible}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Camera" />
                <ComboBox
                    Grid.Column="1"
                    DisplayMemberPath="Description"
                    ItemsSource="{Binding FanfareTypeList}"
                    SelectedValue="{Binding Fanfare}"
                    SelectedValuePath="Value" />
            </Grid>
            <Grid mah:VisibilityHelper.IsVisible="{Binding ShowToggleOption}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding ToggleOptionDescription}"/>
                <mah:ToggleSwitch IsOn="{Binding IsToggled}" Grid.Column="1"/>
            </Grid>
        </StackPanel>

    </GroupBox>
</UserControl>