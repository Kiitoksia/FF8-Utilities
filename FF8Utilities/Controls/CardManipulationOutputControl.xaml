<UserControl x:Class="FF8Utilities.Controls.CardManipulationOutputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FF8Utilities.Controls"
             xmlns:models="clr-namespace:CardManipulation.Models;assembly=CardManipulation"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             PreviewKeyDown="UserControl_PreviewKeyDown"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance models:CardManipulationModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding RareCardTimer}" Foreground="{Binding TextColor}" FontSize="18" FontWeight="Bold"/>
        <TextBlock Text="{Binding Snake}" Foreground="{Binding TextColor}" FontSize="18" FontWeight="Bold" Grid.Column="1"/>

        
        <TextBox Text="{Binding RecoveryPattern}" Controls:TextBoxHelper.Watermark="Enter card patterns and hit enter" x:Name="RecoveryTextBox" Grid.ColumnSpan="2" Grid.Row="1"/>
    </Grid>
</UserControl>
